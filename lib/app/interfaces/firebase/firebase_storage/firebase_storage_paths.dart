String productImagePath(String productId, String fileName) =>
    'product_images/$productId/$fileName';

String productGlbFilePath(String productId, String fileName) =>
    'product_glb_files/$productId/$fileName';
