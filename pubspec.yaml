name: n_sneaker_dashboard
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  cloud_firestore: ^3.1.9
  cloud_functions: ^3.2.9
  firebase_auth: ^3.3.9
  firebase_core: ^1.13.0
  flutter:
    sdk: flutter

dev_dependencies:

flutter:
  uses-material-design: true
